{
  "name": "pos-electron-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "POS Electron Application Monorepo",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --kill-signal SIGTERM --names \"backend,frontend\" --prefix-colors \"blue,green\" \"cd apps/python-backend && . venv/bin/activate && python3 -m uvicorn src.main:app --reload --host 0.0.0.0 --port 8000\" \"pnpm --filter electron-app dev\"",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "turbo": "^2.3.3"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "dependencies": {
    "lucide-react": "^0.548.0",
    "usb": "^2.16.0"
  }
}
